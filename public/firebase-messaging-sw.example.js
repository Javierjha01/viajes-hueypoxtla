// Plantilla del service worker para FCM.
// El archivo real firebase-messaging-sw.js se genera desde .env con:
//   node scripts/generate-firebase-sw.js
// No subas firebase-messaging-sw.js al repositorio (está en .gitignore).
